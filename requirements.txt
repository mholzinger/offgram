# offgram itself is Python standard-library only.
#
# The single dependency is instaloader — and it MUST be the patched fork below.
# Stock instaloader 4.15.1 is broken against current Instagram (retired doc_id /
# GraphQL changes). This fork carries the unmerged fixes.
instaloader @ git+https://github.com/mholzinger/instaloader@offgram-stable

# Optional: enables "⚙ accounts → import" (load a session from a logged-in
# browser via instaloader --load-cookies). Without it, create sessions the normal
# way (instaloader --login). Firefox/Chrome are most reliable; Safari is flaky.
browser_cookie3

# System (non-pip) dependency, optional: ffmpeg — used to generate poster frames
# for videos that have no thumbnail. Install via `brew install ffmpeg`.
